{
  "name": "crds-styles",
  "version": "0.0.1",
  "description": "",
  "style": "assets/stylesheets/_crds.scss",
  "scripts": {
    "build-svg": "./node_modules/svg-sprite/bin/svg-sprite.js --config=config/svg-sprite.config.json assets/svgs/*.svg && ./node_modules/icon-font-generator/bin/icon-font-generator assets/svgs/*.svg -o assets/fonts --csspath assets/stylesheets/components/_iconfont.scss --center -f ../../fonts -p icon",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && npm run build-svg && webpack --config config/webpack.js  --progress --profile --bail"
  },
  "files": [
    "assets"
  ],
  "keywords": [
    "crossroads"
  ],
  "author": "",
  "license": "BSD-3-Clause",
  "dependencies": {
    "bootstrap-sass": "*",
    "svg-sprite": "^1.3.6"
  },
  "devDependencies": {
    "icon-font-generator": "^1.0.7",
    "node-sass": "^4.1.1",
    "raw-loader": "^0.5.1",
    "rimraf": "^2.5.2",
    "sass-loader": "^4.1.1",
    "webpack": "^1.14.0",
    "webpack-merge": "^0.17.0"
  }
}
